import { AngularLoaderDirective } from './angular-loader.directive';

describe('AngularLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AngularLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
